import MagicStringBase from 'magic-string';
import { Node } from '@babel/types';

declare class MagicString extends MagicStringBase {
    removeNode(node: Node): this;
    sliceNode(node: Node, offset?: number): string;
}

export { MagicString };
